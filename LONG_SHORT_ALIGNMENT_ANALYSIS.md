# 长段—多短段失配：观察、机制与算法/模型协同路线

状态：研究分析，不改变当前生产契约
日期：2026-08-29

## 1. 结论摘要

当前 `mdl-v1` 对“双方原始分段大致平行”的依赖，不是一个单独的最大跨度限制，而是以下
四层约束叠加的结果：

1. 原子语义证据在行、列两个方向排名后取较弱值。一个长段对应多个短段时，长段所在的
   排名轴必须给多个真实片段排出互斥名次，除最突出片段外，其余片段容易被系统性降权。
2. 任意 `1:N/N:1` 的原子 MDL 虽没有固定 span cap，但其 span bits 是原子格点证据之和；
   多个被降权的局部格点仍会使正确长 span 缺少支持。
3. 完整拼接块与 leave-one-out 的组合编码只作用于已经由中心窗口、原子路径或相邻 gap
   边界移动提出的候选。它是候选审计器，不是独立的全跨度召回器。
4. 当前路径语法只含 `1:1 / N:1 / 1:N / 1:0 / 0:1`。一般 `N:M` 无论模型多好都
   不可能被忠实输出。

因此，这一问题既不是“只需训练更好的 embedding”，也不是“只需让 DP 搜索更宽”。训练
只能改善可表示候选的证据；候选或语法中不存在的关系无法由训练补回。反过来，无条件扩张
候选也不能解决完整块增益为负、细节删除不降分的问题。

建议先建立“候选 oracle × 模型 checkpoint”的二维消融，再做两项独立原型：

- 算法侧：分段不变的层级候选格 + 可表示 `N:M` 的单调矩形块；
- 模型侧：重分段一致性 + 绝对覆盖缺失反事实训练。

不要先加入最大跨度、长度比、merge penalty 或余弦阈值。它们会把已观察到的表示缺失和
证据缺失隐藏成另一组语料相关超参。

## 2. 2026-08-29 外部结构证据

### 2.1 数据与边界

证据来自三篇公版芥川短篇的日英对齐。自动提议后由 Codex 单次盲审并冻结报告 SHA-256；
它是 AI 审阅参考，不是双人人工 gold。三款模型均以 Q8、相同分段、相同 observed cosine、
候选图和 MDL 路径运行。TE70 与 Phase3 使用 raw input，原始 Harrier 使用其正确
instruction。为避免把 Harrier 专属绝对校准套给微调模型，实验只显式绕过文档接受门控，
不改变排名、候选、组合证据或路径目标。

可复现实验位于 `dualign-embedding-lab` 提交 `8aa63c6`：

```text
docs/evidence/akutagawa-model-alignment-comparison-2026-08-29.json
scripts/evaluate_akutagawa_alignment_models.py
```

整体路径指标显示，公开重训 TE70 的 boundary F1 为 `0.9198`，历史私有文学 Phase3 为
`0.9081`，原始 Harrier 为 `0.9149`。但问题集中在分段最不对称的《蜘蛛之丝》：日文
18 行、英文 38 行；TE70 boundary F1 为 `0.7222`，Phase3 为 `0.6111`。

### 2.2 《蜘蛛之丝》的可表示性

冻结审阅结果共 15 个块，其中包括：

```text
2:5 × 2
2:8 × 1
2:3 × 1
1:4 × 1
1:3 × 1
1:2 × 2
```

四个两侧都多行的块属于一般 `N:M`，当前语法从定义上不能精确恢复。这意味着该篇的
exact-block 与 boundary 指标不仅测模型，也测语法覆盖；不能把全部误差归因于 embedding。

其余有四个可由当前语法表示的复合 gold：三个 `1:2..1:4` 和另一个 `1:2`。对生产管线
内部阶段的只读审计如下：

| 模型 | 中心/原子候选已含 gold | 进入组合审计 | 最终选择 |
| --- | ---: | ---: | ---: |
| TE70 Q8 | 1/4 | 4/4 | 1/4 |
| Phase3 Q8 | 0/4 | 1/4 | 0/4 |
| Harrier Q8 | 3/4 | 4/4 | 1/4 |

这里“进入组合审计”指候选实际出现在 `counterfactual_dld` diagnostics 中，不只是理论上
可枚举。它揭示两个不同瓶颈：

- Phase3 首先有候选召回问题；其唯一进入审计的 `1:3` gold 完整块相对最佳
  leave-one-out 的 gain 为 `-0.01953`，说明模型也没有给出“最后一个片段不可缺”的证据。
- TE70 的候选闭包已经审计全部四个可表示复合 gold，但最终只选择一个。四个 DLD
  diagnostics 的完整块 gain 约为 `-0.00293 / +0.00391 / -0.00586 / +0.04443`；只有
  `+0.04443` 的 `1:2` 进入最终路径。由此可见，单纯扩大候选召回不能解决全部失败。

这组数字是机制定位，不是泛化率。AI 审阅块可能仍有争议，样本也只有一篇强不对称作品；
下一轮必须以人工复核和更多作品确认。

它也意味着先前的三模型对齐表是“模型 × 当前算法”的端到端系统比较，不能直接解释成纯
语义能力排序。模型可以通过改变原子排名间接改善脚手架和候选召回；算法也可能让一个本来
具有完整块信息的模型没有机会给正确关系计分。两者必须通过 oracle 候选消融拆开。

## 3. 失败如何沿当前调用链产生

### 3.1 原子 mutual-rank 隐含局部排他性

`src/dualign/algorithms/mdl/mdl_aligner.py::mutual_rank_code_evidence()` 对每个原子格点
计算行排名与列排名的 Zipf code saving，并取二者较弱值：

```text
e(i,j) = min(row_rank_saving(i,j), column_rank_saving(i,j))
```

对理想 1:1，这一对称保守性有益：一条边必须从双方视角都具有区分度。但若一个长段 `A`
真实对应 `B1+B2+...+Bk`，`A` 所在行仍必须在 `B1..Bk` 之间排出第一、第二直到第 k。
即使每个 `Bj` 都覆盖 `A` 的不同部分，只有少数边能获得很强的双向较弱证据。该统计量把
“多个互补片段”部分误建模成“多个互斥候选”。

### 3.2 任意 span 语法不等于分段不变

`align_evidence_lattice_mdl()` 确实能以 prefix recurrence 搜索任意连续 `1:N/N:1`，没有
旧版 10/20 行 span cap。但复合 span 的语义仍来自原子 evidence 的加和，结构复杂度为
`N-1`。因此“能枚举”不等于“有足够证据入选”：若 `B2..Bk` 的原子 bits 已被排名压低，
正确大 span 会同时承担较弱语义和更高结构码。

### 3.3 脚手架不是硬锚点，但候选覆盖仍由原子几何决定

`pipeline.py` 先从 mutual-rank evidence 求最优单调匹配 `M`，再由
`candidate_graph.py::align_centered_frontier_mdl()` 建立以前后相邻脚手架点为界的中心窗口。
脚手架点本身可以在一个窗口内被跨越，所以它不是旧算法意义上的硬锚点；但局部 Pareto
路径及其支持边仍决定哪些复合关系进入稀疏全局图。正确关系若跨越多个错误脚手架位置，或
在所在窗口的任何非支配路径上都没有出现，就不会自然成为完整块候选。

因此更准确的表述是：当前算法不硬绑定某条初始路径，却仍由原子一对一证据的相对几何来
决定计算窗口和候选支持。

### 3.4 组合层是条件式验证，不是全局召回

`composition_mdl.py::decision_relevant_candidates()` 保留全部 1:1，验证原子路径已选复合
边，并把 gap 的一条边界行吸收到相邻语义边；随后只对已有复合候选递归生成前后缀子块。
该闭包可以从已有大块向内缩，也能完成局部一行边界移动，但不能保证从任意长 gap run
向外生长出正确 `1:k`，更不能生成一般 `N:M`。

进入组合层后，完整拼接块与 leave-one-out 才被真正编码。该设计能避免为所有二次方 span
付费，但也使 joined-block 证据受候选召回条件限制。《蜘蛛之丝》中 TE70 的 4/4 审计、
1/4 选择又说明：即使候选存在，完整块相对直接子块的必要性证据仍可能不足。

### 3.5 硬分隔点 witness 只覆盖极窄结构

`pipeline.py::_hard_boundary_sum_gain()` 只处理：

```text
D = gap + 1:1
P = 2:1（或 1:2）
```

它利用长侧已有硬标点，把完整长句拆为两个片段，判断两条对角线短句得分之和是否改善。
这能解决部分 2:1，但不处理 `1:k (k>2)`、两侧同时重分段、`N:M`，也不主动提出候选。

## 4. 对算法优化的启示

### 4.1 首先建立 segmentation-invariance 测试，而不是立即改目标函数

从已确认的 1:1 gold 出发，只改变分段、不改变字符内容：

1. 在一侧硬分隔点拆成 `k=2..8`；
2. 在两侧使用不同边界拆分，构造真实 `N:M`；
3. 随机合并相邻段，覆盖长短比例和绝对 token 数；
4. 要求粗粒度块边界、匹配/空缺状态和总覆盖在重分段前后保持一致。

该测试能把“语义不会”与“同一内容换了分段就不会”分离，是所有后续算法改动的第一验收
条件。

### 4.2 增加层级分段格，而不是永久预切碎正文

建议把每侧输入表示为包含多个 view 的有向无环格：

- 原始行；
- 硬标点产生的潜在子段；
- 相邻原始行或子段的连续组合；
- 可选的段落级滚动块。

路径选择消费逻辑覆盖区间，而不是直接消费某一个固定 view。这样原始 Markdown 和人工
边界仍是权威，细分只作为隐变量。粗粒度对应先限定单调走廊，细粒度 view 再解释块内结构。

关键是为“选择某个分割方案”支付可解释的结构码，并让不同 view 竞争同一内容覆盖，避免
把多种分割重复计算为多份语义证据。

### 4.3 把候选召回与候选判定分开

当前组合编码同时受原子候选约束。更合理的两阶段接口是：

1. **召回层**追求 gold span 高召回，可使用较宽松的单向覆盖、粗块 embedding、硬边界格和
   相邻 gap run；
2. **判定层**继续使用 DLD/posterior、覆盖证据和结构码抑制错误合并。

召回层不应使用固定 `max_span` 或 length-ratio 阈值。可先研究两种无经验阈值方案：

- 对相邻 gap run 做惰性前缀/后缀扩张，只要该区域仍存在 D/P 路径分歧就继续；
- 为尚未展开的更大 span 建立语义 bits 的可采纳上界，以 branch-and-bound 证明其不可能
  翻转当前最优路径后停止。

### 4.4 为一般 N:M 增加忠实表示

《蜘蛛之丝》的主要 gold 不是单侧 merge，而是 `2:5 / 2:8`。有两条研究路线：

- **矩形块边**：一个关系直接消费连续 source interval 与 target interval，以 joined-block
  和块内覆盖证据计分；输出保留 `N:M`。
- **潜在事件节点**：两侧各自的连续 span 对齐到同一隐含语义块，块内可再选择细粒度路径；
  对外仍输出一个矩形关系和可选的内部解释。

不能简单把 N:M 拆成若干 1:N/N:1 并把内部边界当真，因为当前审阅已经表明其中一些块只在
整体层面可确定。矩形块会扩大候选空间，必须与层级走廊、惰性展开和结构码共同设计。

### 4.5 泛化硬分隔点证据

现有 `2:1/1:2` witness 可推广为：给定一个候选矩形块，在双方硬边界格上寻找单调片段匹配，
比较“分片对角覆盖”与“各片段面对完整长段”的描述长度。内部最优分割应计入选择码长，且
失败只表示没有新增证据，不应自动否定整体块。

这项证据适合作为矩形候选的内部解释器，不适合作为脱离路径上下文的一般 merge 分类器。

### 4.6 文档门控也需要重分段压力测试

存在性统计和 `U/M` 顺序损失同样建立在原子矩阵上。当前实验用 diagnostic gate bypass
隔离了路径质量，尚未证明强不对称分段下生产门控稳定。应在同一文档的多种重分段 view 上
测试 gate 决策与 p 值漂移；若不稳定，可让门控同时观察原始行与粗块 view，而不是放宽
Harrier 校准或共享跨模型 null。

## 5. 对模型训练的启示

### 5.1 训练目标应是分段不变与覆盖敏感，而非普遍抬高长短文本 cosine

盲目提高 `sim(long, short_fragment)` 会让同主题局部片段、译注和漏译更容易被误合并。
模型需要同时满足：

```text
同一完整语义块的不同分段 view：保持接近
完整块 vs 缺失/插入/替换片段：保持可分
```

即“对边界变化不敏感，对信息覆盖变化敏感”。

### 5.2 正例构造

从高置信平行块或人工 gold 出发：

- 单侧在真实硬分隔点拆成 `1:k`，另一侧保持完整；
- 双侧以不同边界拆分成 `N:M`；
- 相邻已确认关系合并为更长块，再随机选择不同粒度 view；
- 覆盖多语言、不同文体，以及 256--2k token 的单侧长文本；
- 同一个语义块保留 whole/segments/joined-segments 多个正 view，而不是只保存一种字符串。

这类数据主要教“任务与分段关系”，不需要依赖受版权保护的小说正文；公版文学、国际组织
多语文档和公开法案都适合。

### 5.3 负例构造

在完整块的开头、中间和结尾分别执行：

- 删除一个或多个真实片段；
- 插入相邻但不属于本块的内容；
- 用同主题、同实体但事实不一致的片段替换；
- 只保留摘要、细节或一个局部事件；
- 加入译注、标题、日期、说话人或重复修饰语。

长度分层必须同时覆盖“固定绝对删除量”和“固定相对删除比例”。目标不是让固定信息损失的
分数惩罚完全与文本长度无关，而是降低其随长度增长而衰减的速度，例如从近似线性稀释改善
为更缓慢的次线性或对数式衰减。

### 5.4 损失函数与指标

仅用普通 in-batch contrastive loss 不足以表达一个块内多个互补片段。建议并行评估：

- **重分段一致性损失**：whole 与同内容不同切分/重组 view 的 embedding 接近；
- **完整性排序损失**：full block 高于每个 leave-one-out、insertion、replacement；
- **集合覆盖损失**：正确矩形块的片段匹配应覆盖双方，而不是由单个最高相似片段代表全部；
- **多正例对比**：同一 gold block 的多个 view 是一组 positives，避免它们在 batch 内互为负例；
- **长度分桶 margin**：分别报告 256/512/1024/2048 token 及删除绝对量，防止平均指标掩盖
  长文本稀释。

训练验收不能只看 cosine 或句对 Top-1。至少保留：candidate recall、merge/split gain、
完整块相对最佳消融 margin、路径 boundary F1、匹配/空缺准确率、N:M block recall 和
重分段前后路径一致率。

### 5.5 当前两款模型给出的训练线索

- Phase3 在分段接近历史小说语料的《鼻子》中正确恢复两处 TE70 错拆的 2:1，说明文学域
  训练确实能提高特定复合关系证据。
- Phase3 在《蜘蛛之丝》初始 4 个可表示复合 gold 中召回 0 个，唯一进入组合审计的完整块
  gain 仍为负，说明这种能力没有泛化到更强的分段不对称。
- TE70 的组合层能审计 4/4，但只有一个完整块 gain 足以改变路径，说明公开重训改善了候选
  稳定性，却尚未解决绝对覆盖与长文本平均效应。

这支持把下一阶段训练数据按“语义内容”和“分段 view”解耦：同一底层 gold block生成多种
分段、合并和反事实覆盖样本，直接学习不变性，而不是继续堆同一种 1:1 文学句对。

## 6. 建议的可证伪消融顺序

### Phase A：不改生产算法，建立上界

对每个冻结案例运行四格矩阵：

| | 当前候选 | oracle 注入 gold span |
| --- | --- | --- |
| 原始/TE70/Phase3 模型 | 现有端到端结果 | 隔离候选召回损失 |
| 新训练 checkpoint | 测训练是否改善自然候选 | 测训练是否改善组合判定 |

同时另算“当前语法可表示 gold”与“一般 N:M gold”，避免把不可表示关系计作模型错误。

判读：

- oracle 后恢复：主要是候选算法问题；
- oracle 后仍失败但新模型恢复：主要是模型覆盖证据问题；
- 所有模型 oracle 后仍失败：检查结构码/组合码；
- N:M：在语法扩展前没有可恢复上界。

### Phase B：重分段 benchmark 与模型训练

先冻结公版语料的 resegmentation benchmark，再训练；dev/test 必须按作品隔离，不能让同一
底层文本的不同切分 view 跨 split。用当前算法与 oracle 候选同时评估，防止训练收益被候选
瓶颈遮蔽。

### Phase C：算法原型

按以下最小递增顺序实现研究分支：

1. 相邻 gap run 的惰性 `1:k/k:1` 候选扩张；
2. 多分段 view 的粗到细走廊；
3. 单调 N:M 矩形块；
4. 泛化硬边界内部解释。

每一步都对 Phase A/B 的冻结集合做路径级消融，并记录候选数、组合编码数、时间和显存。
只有在独立数据上同时提高候选召回和最终 precision，才进入生产校准。

## 7. 近期不应做的事

- 不因《蜘蛛之丝》单例直接调低 merge penalty 或 gap penalty；当前目标没有这些经验参数。
- 不把 `max_span=8`、长度比或固定 cosine 作为生产答案；它们不能解决 2:8 的 N:M，也会
  随语言与 tokenizer 改变。
- 不把所有文本预切成最短句。过度切分会放大重复、短应答和低信息片段的多解性，并扩大矩阵。
- 不把 Harrier 的门控校准复用于 TE70/Phase3；坐标系和 instruction 身份不同。
- 不用当前三篇 AI 审阅集宣称统计显著或 SOTA；它的作用是暴露机制并设计下一轮实验。

## 8. 代码定位

- 原子 mutual-rank 与任意单侧 span：`src/dualign/algorithms/mdl/mdl_aligner.py`
- 单调证据脚手架：`src/dualign/algorithms/mdl/robustness.py`
- 中心窗口与候选支持边：`src/dualign/algorithms/mdl/candidate_graph.py`
- 决策相关候选与组合 DLD/posterior：`src/dualign/algorithms/mdl/composition_mdl.py`
- 窄 `2:1/1:2` 硬边界 witness：`src/dualign/algorithms/mdl/pipeline.py`
- 当前生产能力边界：`docs/algorithm.md`、`docs/research-alignment-pipeline.md`
